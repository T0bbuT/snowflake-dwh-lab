/*
================================================================================
アカウントのログ出力先設定
================================================================================
前提: ensure_event_table.sql でログ保存先を作成済みであること。
初回構築・DB再構築後、またはログ出力先を設定し直す場合に実行する。
アカウント全体の出力先を変更するため、既存の設定を確認してから実行する。
通常のデータ更新・プロシージャ更新では実行しない。
================================================================================
*/

USE ROLE ACCOUNTADMIN;

ALTER ACCOUNT SET EVENT_TABLE = 'DATA_WAREHOUSE.STAGING.LOAD_EVENTS';

USE ROLE SYSADMIN;
