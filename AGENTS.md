# このリポジトリでの作業指針

## プロジェクトの前提

- ERP・CRMのCSVから、Snowflake上にDWHを構築するプロジェクト。
- SQLはSnowflakeの構文で記述する。元教材のSQL Server向けT-SQLをそのまま適用しない。
- ローカルからのSQL実行とCSVアップロードにはSnowflake CLI（`snow`）を使う。
- Bronzeは元データの取り込み、Silverはクレンジング・標準化、Goldは分析用ビューを担当する。
- 現在のロードは全件入れ替え方式で、履歴管理は扱わない。

## 参照資料

- 構築・更新の実行順序とコマンド: [docs/setup.md](docs/setup.md)
- CSVアップロード: [docs/load-local-csv.md](docs/load-local-csv.md)
- 命名規則: [docs/naming_conventions.md](docs/naming_conventions.md)
- Goldのデータ定義: [docs/data_catalog.md](docs/data_catalog.md)
- 実機検証の結果・範囲・確認事項: [docs/validation-2026-09-13.md](docs/validation-2026-09-13.md)

## Snowflakeでの実機検証

- 原則として専用の検証DBを使用する。SQL内の完全修飾名も含め、参照先を検証DBに合わせる。
- `init_databases.sql` は既存DBを置き換える。Bronze / SilverのDDLはテーブルを、`init_stage.sql` はアップロード済みファイルを含むステージを再作成するため、既存環境で不用意に実行しない。
- ロードプロシージャも各テーブルを `TRUNCATE` する。実行前に対象DBを確認する。
- `init_event_table.sql` はアカウント全体のログ出力先を変更する。検証ではDB単位の設定を使うなど、既存環境への影響を避ける。
- 検証用に実行方法を変更した場合は、その差分と未検証の範囲を報告する。
- 終了後は今回作成した検証用オブジェクトを片付ける。保持する必要がある場合は、その理由と対象を報告する。

## 検証結果の判断

- 変更内容に応じた検証を行う。SQLの実行成功と、データ品質の確認をそれぞれ評価する。
- `LOAD_BRONZE()` / `LOAD_SILVER()` は失敗時に `ERROR: ...` を返すため、CLIの終了コードだけでなく戻り値が `SUCCESS` かを確認する。
- ロード変更時は対象レイヤと下流の件数・品質チェック・ログを確認する。全件入れ替えの変更時は再ロードも確認する。
- 品質チェックは各クエリの目的と期待値を読む。分類値の一覧や確認対象の抽出は、行が返るだけで不具合とは判断しない。
- チェックを通すためだけに元CSVや変換ルールを変更しない。未決定の業務ルールは確認事項として報告する。
- 実機検証を実施していない場合は明記し、静的確認のみで動作確認済みとしない。

## ドキュメントと作業ファイル

- セットアップ順序・前提・戻り値を変更したら、関連ガイドも更新する。
- 詳細な手順や検証結果は `docs/` に記録し、このファイルには共通の作業指針と参照先を保つ。
- `TODO.md`、`scratchpad.sql`、`.cortex/`、`.codex/` はローカル用として扱い、明示的な依頼がなければコミット対象に含めない。
- ユーザーの作業中の変更を保持し、コミットは変更の目的ごとにまとめる。
- コミットメッセージはConventional Commits形式（`type(scope): 説明`。scopeは任意）に従い、説明と本文は日本語で記述する。例: `feat: ロードログをEvent Tableへ記録する`、`docs: セットアップ手順を更新する`。
